// Iterative Fibonacci implementation
fn fibonacci(n: felt) -> felt {
    let current = 0;
    let next = 1;

    let counter = 0;
    while counter != n {
        let new_next = current + next;
        current = next;
        next = new_next;
        counter = counter + 1;
    }

    return current;
}
