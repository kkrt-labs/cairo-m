// Test const declarations with explicit type annotations

// Basic typed const
const SIZE: felt = 3;
const MAX: u32 = 100;

// Type checking should validate the annotation matches the value
const PI: felt = 314;  // OK: literal infers as felt

// Use typed const in expressions
fn test() -> felt {
    return SIZE + 1;
}

fn test_u32() -> u32 {
    return MAX;
}